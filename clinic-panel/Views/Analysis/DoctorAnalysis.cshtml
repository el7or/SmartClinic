@model clinic_panel.DTOs.AnalysisDoctorDTO

@{
    ViewBag.Title = "DoctorAnalysis";
}

<h1>قائمة التحاليل للطبيب: @ViewBag.DoctorName</h1>

<div class="row">
    <div class="col-md-6">
        <p>
            @Html.ActionLink("إضافة تحليل", "CreateDoctorAnalysis", new { id = Model.DoctorId })
        </p>

        <table class="table">
            <tr class="info">
                <th>
                    التشخيص
                </th>
            </tr>

            @foreach (var item in Model.AnalysisNames)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Analysis)
                    </td>
                </tr>
            }
        </table>
    </div>
</div>

<div>
    @Html.ActionLink("رجوع", "Index", "Doctor")
</div>

