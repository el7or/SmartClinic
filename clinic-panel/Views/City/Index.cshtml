@model IEnumerable<clinic_panel.Models.SysGovernoratesValue>

@{
    ViewBag.Title = "المحافظات";
}

<h2>قائمة المحافظات</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr class="info">
        <th>
            المحافظة/عربي
        </th>
        <th>
            المحافظة/إنجليزي
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text_ar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text_en)
        </td>
        <td>
            @Html.ActionLink("المدن التابعة لها", "Details", new { id = item.Id }) @*|
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
        </td>
    </tr>
}

</table>
