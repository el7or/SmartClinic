@model clinic_panel.DTOs.MedicineIndexDTO

@{
    ViewBag.Title = "الأدوية";
}

    <div class="row">
        @*<div class="col-md-4">
            <h2>أسماء الأدوية</h2>

            <p>
                @Html.ActionLink("إضافة دواء", "CreateMedicineName")
            </p>

            <table class="table">
                <tr class="info">
                    <th>
                        اسم الدواء
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model.MedicineNames)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Text)
                        </td>
                        <td>
                            <a href="@Url.Action("EditMedicineName",new { id= item.Id })">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </a>
                        </td>
                    </tr>
                }
            </table>
        </div>*@        
        <div class="col-md-3">
            <h2>الكميات</h2>

            <p>
                @Html.ActionLink("إضافة كمية", "CreateMedicineQuantity")
            </p>

            <table class="table">
                <tr class="info">
                    <th>
                        الكمية
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model.MedicineQuantities)
                {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    <a href="@Url.Action("EditMedicineQuantity",new { id= item.Id })">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                </td>
            </tr>
                }
            </table>
        </div>
        <div class="col-md-3">
            <h2>جرعات الأدوية</h2>

            <p>
                @Html.ActionLink("إضافة جرعة", "CreateMedicineDose")
            </p>

            <table class="table">
                <tr class="info">
                    <th>
                        الجرعة
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model.MedicineDoses)
                {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    <a href="@Url.Action("EditMedicineDose",new { id= item.Id })">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                </td>
            </tr>
                }
            </table>
        </div>
        <div class="col-md-3">
            <h2>توقيتات الدواء</h2>

            <p>
                @Html.ActionLink("إضافة توقيت", "CreateMedicineTiming")
            </p>

            <table class="table">
                <tr class="info">
                    <th>
                        التوقيت
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model.MedicineTimings)
                {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    <a href="@Url.Action("EditMedicineTiming",new { id= item.Id })">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                </td>
            </tr>
                }
            </table>
        </div>
        <div class="col-md-3">
            <h2>فترات العلاج</h2>

            <p>
                @Html.ActionLink("إضافة فترة", "CreateMedicinePeriod")
            </p>

            <table class="table">
                <tr class="info">
                    <th>
                        الفترة
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model.MedicinePeriods)
                {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Text)
                </td>
                <td>
                    <a href="@Url.Action("EditMedicinePeriod",new { id= item.Id })">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                </td>
            </tr>
                }
            </table>
        </div>
    </div>
