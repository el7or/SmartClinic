@model IEnumerable<clinic_panel.DTOs.SpecialtyIndexDTO>

@{
    ViewBag.Title = "التخصصات";
}

    <h2>التخصصات</h2>

<p>
    @Html.ActionLink("إضافة تخصص", "Create")
</p>

<table class="table">
    <tr class="info">
        <th>
            التخصص
        </th>
        <th>
           عدد الأطباء
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Text)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DoctorsCount)
    </td>
    <td>
        @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
    </td>
</tr>
}

</table>
